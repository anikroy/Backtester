import java.awt.event.*;
import javax.swing.*;
import java.awt.*;

public class Screen extends JFrame{
	private JPanel panel=new JPanel(),
			       LogPanel=new JPanel(),
			       CandlePanel=new JPanel();
	
	public Screen(){
		super("first");
		
		LogPanel.add((new Logs()).Trade_Log()); 
		
		CandlePanel.add((new Draw()).Price_Chart());
		CandlePanel.setLayout(new BoxLayout(CandlePanel, BoxLayout.Y_AXIS));
		
		panel.setBackground(Color.black);
		panel.setLayout(new GridLayout(4,4));
		
		
		//panel.add(CandlePanel).setLocation(0,0);
		panel.add(LogPanel).setLocation(0,0);
		panel.add(LogPanel).setLocation(1,0);
		panel.add(LogPanel).setLocation(0,1);
		panel.add(LogPanel).setLocation(1,1);
		
		
		this.setContentPane(panel);
	}
	
}
